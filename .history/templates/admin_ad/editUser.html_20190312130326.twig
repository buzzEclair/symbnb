{% extends 'admin_ad/base.html.twig' %}

{% block body %}
<div class="container">  
        <div class="row">
            <div class="col-sm-9 col-md-9 col-lg-9 mx-auto">
                <div class="card card-signin my-5">
                <div class="card-body">
                    <h5 class="card-title text-center">Modifier votre profile!</h5>
    
                    {{ form_start(form)}}

                    {{form_widget(form)}}

                    <hr class="my-4">
                    <button class="btn btn-lg btn-primary btn-block text-uppercase" type="submit">Modifier les informations</button>
                    {{form_end(form)}}
                </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}